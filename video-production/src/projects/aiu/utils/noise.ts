/**
 * Noise utilities — re-exported from common library.
 * This shim maintains backward compatibility with existing AI&U imports.
 */
export {
  smoothNoise,
  animatedNoise,
  floatingOffset,
  hueShift,
  noise2D,
  noise3D,
} from '../../../common/effects/NoiseTexture';
