/**
 * Motion blur — re-exported from common library.
 * This shim maintains backward compatibility with existing AI&U imports.
 */
export {
  CameraMotionBlur,
  Trail,
  MOTION_BLUR_PRESETS,
} from '../../../common/effects/MotionBlur';
